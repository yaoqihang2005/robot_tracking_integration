# 项目规范 (Robot Tracking Integration)

## 环境准则
- **Conda 环境**: 必须在 `conda activate tracking` 激活后执行脚本。
- **路径约束**: SAM2 位于 `externals/SAM2`，SpaTracker 位于 `externals/SpaTracker`，不要移动它们。
- **禁止操作**: 禁止执行 `rm -rf` 或修改 `/data/` 下非本项目目录的其他文件夹。
- **Python 命令**: 默认使用 `python3`，不要假设 `python` 命令存在。

## 开发风格
- **语言**: 优先使用中文注释和回复。
- **架构**: 遵循模块化原则，核心逻辑放 `core/`，工具类放 `utils/`。
- **硬件**: 只能使用 GPU 0，不要干扰其他同学的实验。
- **改动原则**: 优先“最小改动 + 可回滚”，不要为了修一个问题顺手重构大量文件。

## Git 准则
- 每次修改代码后，请提醒我 `git add` 并生成 Commit Message，但由我手动执行 `git push`。
- **实验分支**: 任何试验性改动都在新分支上进行（例如 `exp/<topic>`），避免污染主线。
- **安全回退**: 对不确定的清理/重构，先打 tag（例如 `before_cleanup_<date>`）再开始。

## 脚本开发规范
- **不要硬编码路径和参数**：所有需要配置的参数（如视频目录、文件路径、坐标等）都应该通过命令行参数传递，不要写死在代码里。
- **使用 argparse**：所有 Python 脚本都应该使用 `argparse` 来接收和解析命令行参数。
- **默认值要合理**：参数应该设置合理的默认值，但允许用户通过命令行覆盖。
- **入口要纯粹**: `scripts/` 与根目录脚本只负责参数解析、校验、调用 `core/`，不要在脚本里堆复杂逻辑。
- **禁止脚本互相依赖**: 不要出现 `from scripts.xxx import yyy` 或脚本之间互相 import；共享逻辑必须下沉到 `core/` / `utils/`。
- **输出目录约定**: 所有脚本默认输出到 `results/<task_name>/...`，并允许通过参数覆盖输出目录。
- **错误信息**: 对缺依赖/缺文件/无视频等场景给出明确错误提示，并在退出前打印关键输入参数。

## 数据与路径约定
- **输入目录识别**: 对视频目录要写清楚“期望的文件结构与命名”。如果脚本对特定命名有过滤（例如 `images.wrist`），必须通过参数控制开关而不是写死。
- **不修改原始数据**: 默认只读处理输入视频/数据，不覆盖、不原地写回；中间产物写到 `data/temp_*` 或 `results/`。
- **临时目录**: 临时目录应可重复运行（存在则覆盖或清理），避免跑一半残留导致下次结果异常。

## 删除与清理策略（重要）
- **不直接删**: 不确定是否还被依赖的脚本/模块，不要直接删除；先移动到 `scripts/_deprecated/` 或改名为 `*_legacy.py`。
- **删除前检索依赖**: 删除/移动前必须全仓库检索引用（import、调用、README/脚本命令），确认无人使用再删。
- **保留可恢复路径**: 真删除前先确保 Git 历史可追溯，必要时打 tag；不要依赖“我记得能找回来”。
- **提交粒度**: 清理提交与功能提交分开，不要混在一个提交里。

## 运行方式（最常用）
- **环境自检**: 所有涉及 SAM2/SpaTracker 的入口脚本应优先调用 `core.config.check_env()` 并在失败时退出。
- **推荐命令格式**: `python3 <script>.py --help` 查看参数；常用参数包含 `--video_dir`、`--output_dir`、`--limit/--num_videos` 等。
- **交互脚本提醒**: 需要 GUI/浏览器交互的脚本，要在启动前提示（例如 headless 服务器可能无法打开窗口/浏览器）。

## AI 改码约束（写给自动改代码的助手）
- **先找再改**: 修改/重命名任意函数、脚本入口或参数前，先全局检索调用点并同步更新。
- **先保留再替换**: 需要替换旧脚本链路时，先做到“新旧并存且可切换”（参数开关/新入口脚本），确认稳定后再清理旧实现。
- **不引入新依赖**: 不要新增未在项目中出现过的第三方库；若必须新增，先更新依赖说明并提供安装方式。
